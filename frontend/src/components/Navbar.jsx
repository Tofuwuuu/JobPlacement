const Navbar = () => {
    return (
        <nav style={{ background: "#333", padding: "10px", color: "#fff" }}>
            <h1>Job Placement System</h1>
        </nav>
    );
};

export default Navbar;
